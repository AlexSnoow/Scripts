{
  "General": {
    "JobName": "BackupAllJson",           // Имя родительского задания. Используется для темы письма, имени лог-файла и источника событий Windows.
    "Domain": "localdomain.loc",          // Домен для формирования email адресов.
    "SmtpServer": "smtp.localdomain.loc", // Адрес SMTP сервера.
    "LogDaysOld": 30,                     // Срок хранения лог-файлов (дней).
    "LogKeepCount": 7,                    // Минимальное количество логов, которые нужно сохранить, даже если они старые.
    "ArchiverType": "RAR",                // Тип архиватора: "RAR", "7Z" или "ZIP".
    "DefaultRarParameters": [ ... ],      // Параметры RAR по умолчанию (если не указаны в Job).
    "Default7zParameters": [ ... ]        // Параметры 7z по умолчанию.
  },
  "Paths": {
    "LogPathRoot": "C:\\work\\...\\logs", // Папка для хранения логов скрипта и списков файлов.
    "NetLogPath": "...",                  // (Опционально) Путь к сетевому хранилищу логов.
    "RarPath": "c:\\work\\rar.exe",       // Путь к исполняемому файлу rar.exe.
    "SevenZipPath": "c:\\work\\7z.exe"    // Путь к исполняемому файлу 7z.exe.
  },
  "Recipients": {
    "AdminIS": "user1@head...",           // Email системного администратора (получает уведомления об ошибках и успехе).
    "AdminOS": "user2@head...",           // Email ответственного сотрудника.
    "AdminMail": "user3@head..."          // (Опционально) Дополнительный email.
  },
  "Integrity": {
    "RarExeHash": "A7A...",               // SHA256 хеш файла rar.exe для проверки безопасности перед запуском.
    "SevenZipExeHash": "4CD7..."          // SHA256 хеш файла 7z.exe.
  },
  "Jobs": [ // Массив заданий. Каждое задание выполняется последовательно.
    {
      "Name": "JOB1",                    // Уникальное имя задания. Подставляется в {JobName}.
      "Source": "C:\\Work\\...\\JOB1\\", // Что архивируем.
      
      // === Шаблон имени архива ===
      "ArchivePattern": "{PCName}_{JobName}_{Date_Time}.rar", 
      // Поддерживаемые переменные:
      // {PCName} - Имя компьютера.
      // {JobName} - Имя этого задания (например "JOB1").
      // {Date} - YYYYMMDD.
      // {Time} - HHMMSS.
      // {Date_Time} - YYYYMMDD_HHMMSS.

      // === Назначение ===
      "LocalDest": "C:\\Work\\...\\Local\\JOB1\\",  // Локальная папка для архива.
      "RemoteDest": "C:\\Work\\...\\Remote\\",      // Сетевая папка (если недоступна, скрипт ругается, но сохраняет локально).
      
      // === Список файлов (опционально) ===
      "ListSourceFlag": "csv",          // Создать список файлов источника.
                                         // "txt" - текстовая таблица.
                                         // "csv" - формат CSV (открывается в Excel).
                                         // Отсутствует/другое - не создавать.
                                         
      // === Проверка масок (опционально) ===
      "SourceCheckMasks": [ "File_0*.*", "*.xml" ], 
      // Массив строковых масок. Скрипт проверит наличие файлов по каждой маске.
      // Если файлов по маске 0 -> ОШИБКА в лог и письмо. Полезно для контроля, что источник не пуст.

      // === Ротация источника (опционально) ===
      "RemoveSourceFlag": false,        // true = удалять старые файлы в источнике.
      "SourceDaysOld": 2,               // Удалять файлы старше N дней.
      "SourceKeepCount": 301,           // Сохранять минимум N файлов.

      // === Ротация архива ===
      "LocalDestDaysOld": 2,            // Хранить локальные архивы N дней.
      "LocalDestKeepCount": 1,          // Минимум N локальных архивов.
      "RemoveRemoteDestFlag": true,     // true = удалять старые архивы в сети.
      "RemoteDestDaysOld": 31,          // Хранить сетевые архивы N дней.
      "RemoteDestKeepCount": 30,        // Минимум N сетевых архивов.

      // === Параметры архивации ===
      "ArhParameters": [ "a", "-m3", "-s", "-ep1", "-dh", "-rr1p", "-r" ], // Переопределение параметров RAR для этого задания.
      "ArhLog": true                    // true = создавать отдельный лог-файл архиватора (.log).
    }
  ]
}
