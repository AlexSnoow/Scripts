Итак, сначала разберусь с тем, что именно хочет получить пользователь. Судя по всему, речь идет о разработке четырех отдельных PowerShell-скриптов, каждый из которых решает свою специфическую задачу. Первый скрипт называется Create_Backup_Rar.ps1 и предназначен для создания резервных копий в формате RAR. Пользователь отметил, что этот скрипт уже готов, значит, его создавать заново не придется. Второй скрипт — MultiBackup.ps1 — служит для пакетной архивации сразу нескольких источников данных, причем для каждого источника предусмотрены индивидуальные настройки. Третий скрипт, Remove-OldFiles.ps1, занимается управлением старыми файлами, позволяя удалять ненужные копии, но сохраняя минимальное необходимое количество. Последний скрипт, Send-Mail.ps1, используется для отправки электронной почты с различными дополнительными возможностями вроде вложений и HTML-оформления.

Пользователь предоставил довольно подробную документацию ко второму, третьему и четвертому скриптам, включая описание назначения, синтаксис, примеры использования и даже некоторые технические детали. Это здорово упрощает задачу, ведь теперь понятно, каким функционалом должны обладать эти скрипты и как они должны выглядеть в конечном итоге.

Теперь важно правильно оформить сам запрос к LLM-моделью. Для этого лучше всего подойдет четкое техническое задание, которое охватывает все необходимые моменты: название проекта, перечень скриптов с их назначением, структуру каталогов, требования к параметрам и функциональности, а также дополнительные пожелания по оформлению и стилю кода. Такой подход позволит избежать недопонимания и обеспечит получение качественных и готовых к использованию скриптов.

Поскольку первый скрипт уже существует, его можно оставить за скобками текущего задания, сосредоточившись на трех оставшихся. Важно учесть, что документация предоставлена на русском языке, поэтому и итоговый запрос тоже должен быть русскоязычным, чтобы сохранить ясность и точность формулировок.

В целом, структура будущего технического задания выглядит вполне понятной и удобной для дальнейшей реализации.

Да, понимаю вашу задачу и помогу вам составить чёткий и структурированный запрос для создания указанных PowerShell-скриптов. Вот подготовленная версия вашего запроса:

---

## 📁 Техническое задание на разработку PowerShell-скриптов

### 🖥 Общая цель:
Разработка комплекта PowerShell-скриптов для автоматизации процессов резервного копирования, управления файлами и отправки уведомлений по электронной почте.

---

### 🗃 Структура каталога скриптов:
```
.\Scripts\
├── MultiBackup.ps1                # Основной скрипт аркестратор для запуска остальных файлов
├── Create_Backup_Rar.ps1          # Уже существующий скрипт для создания одиночных архивов
├── MultiBackup_config.json        # файл для хранения конфигурации, указывает какие файлы запускать и с какими параметрами
├── Copy-Robocopy.ps1              # Файл для копирования данных
├── Remove-OldFiles.ps1            # Функциональный скрипт чистки старых файлов
└── Send-Mail.ps1                  # Функциональный скрипт отправки почтовых сообщений
```
---

### 🧩 Требуемые скрипты и их функциональность:

#### 1. 📄 `Create_Backup_Rar.ps1`
Скрипт уже реализован и проверен. Его задача заключается в создании одного архива формата RAR с возможностью передачи параметров для архивируемых объектов.

---

#### 2. 📄 `MultiBackup.ps1`

- **Назначение:** Выполнение пакетной архивации нескольких источников данных с использованием индивидуальных настроек для каждого источника.
- **Параметры:**
  - `-ConfigPath`: путь к внешнему JSON-конфигурационному файлу (необязательный параметр).
[
    {
        "Enabled": true,
        "Name": "Резервная копия 1",
        "Create_Backup_Rar": true,
        "Create_Backup_Rar_SRC": "C:\\test\\backup1",
        "Create_Backup_Rar_DST": "C:\\test\\rar",
        "Create_Backup_Rar_ArchiveName": "backup1-{datetime}",
        "Create_Backup_Rar_Keys": "a -r -m5 -dh -ep1",
        "Create_Backup_Rar_ArchiveExtension": "rar",
        "Copy-Robocopy": true,
        "Copy-Robocopy_SRC": "C:\\test\\rar",
        "Copy-Robocopy_DST": "C:\\test\\copy",
        "Copy-Robocopy_RobocopyPath": "C:\\Windows\\System32\\Robocopy.exe",
        "Copy-Robocopy_LogName": "copy_log_{datetime}",
        "Copy-Robocopy_LogPath": "C:\\test\\copy",
        "Copy-Robocopy_Keys": "/MIR /Z /R:3 /W:10",
        "Copy-Robocopy_CheckFreeSpace": false,
        "Remove-OldFiles": true,
        "Remove-OldFiles-Path": "C:\\test\\rar",
        "Remove-OldFiles-DaysOld": "30",
        "Remove-OldFiles-KeepCount": "5",
        "Send-Mail": false,
        "Send-Mail-From": "Fromadrres@domail.loc",
        "Send-Mail-To": "toadrres@domail.loc",
        "Send-Mail-Subject": "Резервная копия 1"
    }
]
- **Примеры использования:**
  ```powershell
  # Использование встроенной конфигурации
  .\MultiBackup.ps1

  # Использование внешнего конфига
  .\MultiBackup.ps1 -ConfigPath "C:\my_config.json"

  # Планировщик заданий
  Program: powershell.exe
  Arguments: -ExecutionPolicy Bypass -File "C:\Scripts\MultiBackup.ps1" -ConfigPath "C:\BackupConfigs\daily.json"
  ```

---

#### 3. 📄 `Remove-OldFiles.ps1`

- **Назначение:** Управление хранением файлов по сроку давности с гарантированной защитой минимального числа последних версий.
- **Параметры:**
  - `-Path`: целевая директория (обязательный);
  - `-DaysOld`: максимальный возраст файлов в днях (диапазон 1–3650);
  - `-KeepCount`: минимальное число сохранённых файлов (диапазон 1–1000).
- **Особенности:**
  - Возможность предварительного просмотра операции (`-WhatIf`);
  - Интеграция в другие скрипты.
- **Примеры использования:**
  ```powershell
  # Простой вызов
  Remove-OldFiles -Path "D:\Backups" -DaysOld 180

  # Предварительная проверка
  Remove-OldFiles -Path "C:\Logs" -KeepCount 10 -WhatIf

  # Вызов из другого скрипта
  try {
      . "C:\Scripts\Remove-OldFiles.ps1"
      Remove-OldFiles -Path "E:\AppLogs" -DaysOld 90 -KeepCount 3
      Write-Host "Очистка завершена успешно"
  }
  catch {
      Write-Error "Ошибка: $_"
  }
  ```

---

#### 4. 📄 `Send-Mail.ps1`

- **Назначение:** Автоматическая отправка электронных писем с поддержкой различных возможностей (HTML, вложения, логирование).
- **Основные параметры:**
  - Обязательные: `-From`, `-To`, `-Subject`, `-Body`;
  - Дополнительные: `-SmtpServer`, `-AttachPath`, `-LogPath`, `-Credential`, `-UseSSL`, `-Port`, `-BodyAsHtml`, `-Priority`, `-Encoding`.
- **Примеры использования:**
  ```powershell
  # Простое письмо
  Send-Mail -From "user1@domain.loc" -To "user2@domain.loc" -Subject "Тест" -Body "Текст письма" -SmtpServer "smtp.domain.loc"

  # Письмо с вложениями и HTML
  Send-Mail -From "report@domain.loc" -To "admin@domain.loc" -Subject "Отчёт" -Body "<h1>Ежедневный отчёт</h1>" -BodyAsHtml -AttachPath "C:\Reports\report.pdf" -SmtpServer "smtp.domain.loc"

  # Несколько адресатов
  Send-Mail -From "service@domain.loc" -To "user1@domain.loc", "user2@domain.loc" -Subject "Уведомление" -Body "Важное сообщение" -SmtpServer "smtp.domain.loc"

  # Командная строка
  .\Send-Mail.ps1 -From "alert@domain.loc" -To "admin@domain.loc" -Subject "Ошибка" -Body "Обнаружена ошибка в системе" -SmtpServer "smtp.domain.loc"

  # Планировщик заданий
  Program: powershell.exe
  Arguments: -ExecutionPolicy Bypass -File "C:\Scripts\Send-Mail.ps1" -From "daily@domain.loc" -To "report@domain.loc" -Subject "Ежедневный отчёт" -Body "Отчёт прикреплён" -AttachPath "C:\Reports\daily.txt" -SmtpServer "smtp.domain.loc"

  # Использование в другом скрипте
  . .\Send-Mail.ps1
  Send-Mail -From "noreply@domain.loc" -To "user@domain.loc" -Subject "Добро пожаловать" -Body "Регистрация завершена" -SmtpServer "smtp.domain.loc"
  ```

#### 5. 📄 `Copy-Robocopy.ps1`
PowerShell-скрипт Copy-Robocopy.ps1 представляет собой функцию для автоматизации копирования файлов и папок с использованием утилиты Robocopy Windows. Основные функциональные особенности:

Автоматическое копирование
Обёртка вокруг robocopy.exe, позволяющая задавать параметры копирования через параметры скрипта (источник, цель, ключи).

Логирование

Генерация лог-файлов с динамическими именами (поддержка плейсхолдеров {date}, {time}).
Сохранение логов в указанной директории 
Предварительная проверка

Проверка существования исходных данных и целевой директории.
Проверка доступного пространства на диске перед копированием (CheckFreeSpace).
Обработка ошибок

Анализ кодов возврата Robocopy (0–7) для определения успешности операции.
Логирование ошибок выполнения и вывод статистики по копированию.
Гибкость использования
Может запускаться как отдельный скрипт или импортироваться как модуль для повторного использования в других скриптах. Поддерживает интеграцию с планировщиком задач.
---

### 🛠 Дополнительные требования:

- Все скрипты должны иметь комментарии на русском языке.
- Должна поддерживаться совместимость с современными версиями PowerShell (PowerShell Core / PowerShell 5+).
- Каждый скрипт должен сопровождаться документацией в стиле help-комментариев PowerShell.
- Реализация должна следовать лучшим практикам написания PowerShell-кода (PSRULES, DSC, etc.)
---